<%= simple_form_for [@project, @proposal] do |f| %>
  <%= f.error_notification %>
  <%= f.input :title %>
  <%= f.input :tagline %>
  <%= f.input :status, collection: ["Draft", "Pending", "Accepted", "Rejected"], prompt: "Choose a Status" %>
  <%= f.button :submit %>
<% end %>
